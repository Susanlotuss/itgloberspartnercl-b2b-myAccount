query getDocuments(
  $schema: String
  $fields: [String]
  $acronym: String
  $where: String
  $page: Int
  $pageSize: Int
) {
  documents(
    schema: $schema
    fields: $fields
    acronym: $acronym
    where: $where
    page: $page
    pageSize: $pageSize
  ) @context(provider: "vtex.store-graphql") {
    fields {
      key
      value
    }
  }
}
